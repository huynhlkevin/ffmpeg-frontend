import { Routes } from '@angular/router';
import { VideoClipperComponent } from './video-clipper/video-clipper.component';

export const routes: Routes = [
  { path: 'video-clipper', component: VideoClipperComponent }
];
